# Allow legacy peer dependency resolution for this project.
# next-auth@4 declares nodemailer@^7 as a peerOptional (only needed for its
# built-in Email provider). This project uses Credentials + Google providers
# and manages its own nodemailer@9 for transactional email, so the conflict
# is safe to bypass.
legacy-peer-deps=true
